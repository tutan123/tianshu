# Downloaded source archives; runtime uses the converted offline mesh.
assets/kenney-furniture.zip
assets/kenney-nature.zip
assets/kenney-furniture/Models/
assets/kenney-nature/Models/

# Generated browser captures.
tests/screenshots/
.playwright-mcp/

# dream-loop working context (target image, baseline captures, job records).
.dream-loop/

# Verification-only dependencies. The game itself needs no install (file:// static page).
node_modules/
